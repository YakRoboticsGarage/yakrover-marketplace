# Slug Registry — canonical mapping of here.now slugs to yakrobot.bid URLs
#
# This is the single source of truth for which slug serves which page.
# Deploy scripts should read from this file rather than hardcoding slugs.
#
# Format: url → slug + source directory
# Updated: 2026-04-12

sites:
  - url: "https://yakrobot.bid/"
    slug: gleaming-sierra-28gb
    source: demo/landing/
    description: Main interactive demo — MDOT I-94 RFQ walkthrough

  - url: "https://yakrobot.bid/demo/"
    slug: sturdy-riddle-vvar
    source: demo/marketplace/
    description: Live auction demo — 100-robot fleet, EAS attestation, 9 RFP presets

  - url: "https://yakrobot.bid/yaml/"
    slug: wistful-mantra-9vnz
    source: demo/explorer/
    description: Product brief — curated view of PRODUCT_DSL_v2.yaml

  - url: "https://yakrobot.bid/pitch/"
    slug: bronze-osprey-nsj6
    source: github.com/rafaeldavid/robotTAM/pitch/
    description: Pitch deck — managed in robotTAM repo

# Archived slugs (no longer actively deployed)
archived:
  - slug: quartz-quail-vcrz
    was: docs/archive/mcp_demo_v2/
    note: MCP demo v2 (superseded by sturdy-riddle-vvar)

  - slug: ubuntu-ledger-w4t9
    was: chatbot/
    note: Early chatbot deploy (superseded by Cloudflare Worker)
